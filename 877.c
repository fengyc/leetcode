#include <stdbool.h>
#include <stdlib.h>

bool stoneGame(int *piles, int pilesSize) {
    int **p = malloc(pilesSize * sizeof(int *));
    for (int i = 0; i < pilesSize; i++) {
        p[i] = calloc(pilesSize, sizeof(int));

    }
    for (int i=0;i<pilesSize;i++) {

    }
}